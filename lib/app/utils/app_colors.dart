import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // private constructor, prevent instantiation

  // Primary
  static const primary = Color(0xFF1E88E5);
  static const primaryLight = Color(0xFF6AB7FF);
  static const primaryDark = Color(0xFF005CB2);

  // Secondary
  static const secondary = Color(0xFFD32F2F);
  static const secondaryLight = Color(0xFFFF6659);
  static const secondaryDark = Color(0xFF9A0007);

  // Background & Surface
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textWhite = Color(0xFFFFFFFF);

  // Success / Error / Warning
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFF44336);
  static const warning = Color(0xFFFFC107);

  // Custom
  static const info = Color(0xFF29B6F6);

  static const customBlue = Color(0xFF112C45);
  static const org = Color(0xFFBB622A);
}
